@model Jogador
@{
    ViewData["Title"] = "Jogadores";
}

<div class="titulo_pagina">
    <h1>cadastro de @ViewData["Title"]</h1>
</div>

<form class="cadastro" method="POST" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">

    <div class="campo">
        <label for="">ID Jogador</label>
        <input type="text" name="Id Jogador" required>
    </div>

    <div class="campo">
        <label for="">ID Equipe</label>
        <input type="text" name="Id Equipe" required>
    </div>

    <div class="campo">
        <label for="">Nome</label>
        <input type="text" name="Nome" required>
    </div>

    <div class="campo">
      <label for="">Email</label>
    <input type="text" name="Email" required>
    </div>
    <div class="campo">
      <label for="">Senha</label>
    <input type="text" name="Senha" required>
    </div>

    <button class="gradient btn" type="submit">Cadastrar</button>
</form>

<table>
    <thead>
        <th>ID</th>
        <th>Nome</th>
        <th>Email</th>
    </thead>
    <tbody>
        @foreach (Jogador j in ViewBag.Jogadores)
        {
            <tr>
                <td>@j.IdJogador</td>
                <td>@j.Nome</td>
                <td>@j.Email</td>
            </tr>
        }
    </tbody>
</table>